# Generated by Anaconda 36.16.5
# Generated by pykickstart v3.43
#version=DEVEL
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=cn --xlayouts='cn'
# System language
lang zh_CN.UTF-8

# Network information
network  --bootproto=static --device=link --gateway=127.0.0.1 --ip=NETWORK_IP --nameserver=8.8.8.8 --netmask=255.255.252.0 --ipv6=auto --activate

# Use hard drive installation media

%post  --nochroot
/bin/bash /run/install/repo/clear_kytuning_efibootmgr.sh &> /var/log/clear_efibootmgr.log

if [[ "NETWORK_IP" == "127.0.0.1" || "NETWORK_IP" == "127.0.0.1" ]]; then
    cp /run/install/repo/ifcfg-enP1p3s0f0 /etc/sysconfig/network-scripts/
fi
%end

%packages
@^server-product-environment
@development

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.6.1
ignoredisk --only-use=SYSTEM_DISK
autopart
# Partition clearing information
clearpart --all --initlabel --drives=SYSTEM_DISK

# System timezone
timezone Asia/Shanghai --utc

# Root password
rootpw --iscrypted PASSWORD
reboot