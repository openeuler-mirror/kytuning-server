#version=RHEL8
# Use graphical install
graphical

%post  --nochroot
/bin/bash /run/install/repo/clear_kytuning_efibootmgr.sh &> /var/log/clear_efibootmgr.log
%end

%packages
#1050a的：1050a/AppStream/repodata/5bc509a40d955f868ea335b91af9b4491a957bee1951d4bb8f16f29f37a589a1-comps-AppStream.aarch64.xml
@^graphical-server-environment-dde
@development

%end

# Keyboard layouts
keyboard --xlayouts='cn'
# System language
lang zh_CN.UTF-8

# Network information
network  --bootproto=static --device=link --gateway=127.0.0.1 --ip=NETWORK_IP --nameserver=8.8.8.8 --netmask=255.255.252.0 --ipv6=auto --activate
network  --hostname=localhost.localdomain

# Use CDROM installation media
#cdrom

# Run the Setup Agent on first boot
firstboot --enable

ignoredisk --only-use=SYSTEM_DISK
autopart
# Partition clearing information
clearpart --all --initlabel --drives=SYSTEM_DISK

# System timezone
timezone Asia/Beijing --isUtc

# Root password
rootpw --iscrypted PASSWORD

%addon com_redhat_kdump --disable --reserve-mb='512'

%end

%anaconda
pwpolicy root --minlen=8 --minquality=1 --notstrict --nochanges --emptyok
pwpolicy user --minlen=6 --minquality=1 --notstrict --nochanges --emptyok
pwpolicy luks --minlen=6 --minquality=1 --notstrict --nochanges --notempty
%end
reboot